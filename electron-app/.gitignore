# Generated public UI ships inside the installer, not in Git.
/ui-bundle/
/ui-bundle.staging-*/

# Credenciales selladas en el build. NUNCA se commitean: la llave viaja en el
# instalador (igual que ya viajaba dentro del ui-bundle), no en el repositorio.
credenciales-selladas.json
ui-bundle.sello.json
