# Copy to a private temp file, fill with staging/demo values, then source it.
# Do NOT commit real passwords.

SUPABASE_URL=https://jkcnxfbbuyyfhwfjizgw.supabase.co
SUPABASE_ANON_KEY=<staging-anon-key>

TENANT_CLIENT_ID=nomada
TENANT_OWNER_EMAIL=owner@nomada.staging
TENANT_OWNER_PASSWORD=<demo-password>

EXPECTED_OTHER_CLIENT_ID=stg_a
EXPECT_MENU_MIN=1
EXPECT_STAFF_MIN=1

# Run:
#   set -a
#   source /private/tmp/tenant_jwt_smoke.env
#   set +a
#   python3 scripts/onboarding/tenant_jwt_smoke.py
