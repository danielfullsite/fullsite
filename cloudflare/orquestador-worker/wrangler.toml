name            = "telegram-orquestador-warroom"
main            = "src/index.ts"
compatibility_date = "2025-01-01"
account_id      = "fd00e9e6edd331e0abe6d1e796f38172"

[observability]
enabled = true   # logs visibles en wrangler tail

# === Secrets requeridos (setear con `wrangler secret put NOMBRE`) ===
# - TELEGRAM_BOT_TOKEN — del @BotFather
# - WEBHOOK_SECRET — token aleatorio para verificar webhooks
# - ANTHROPIC_API_KEY — para llamadas a Claude (chat libre)
# - SUPABASE_URL — https://qjiomlvudfmzuvqvhwpk.supabase.co
# - SUPABASE_SERVICE_KEY — service_role key (NO la anon)
# - CHAT_ID_DANIEL — Telegram chat_id de Daniel
# - CHAT_ID_MONICA — Telegram chat_id de Mónica
