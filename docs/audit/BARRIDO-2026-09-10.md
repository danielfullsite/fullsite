# Barrido de defectos del 10 de septiembre — matriz por módulo

Rama `cert/instalador-2026-09-10` (PR #392). Base del barrido: `c2cafac9`.
Método: doce lentes de búsqueda con refutación adversarial independiente; sólo
entran los hallazgos que sobrevivieron la refutación y que se reprodujeron con
una prueba antes de corregirse (A/B: la prueba nueva falla con el código
anterior y pasa con el nuevo). Vocabulario del estado según el protocolo del
repo: **implementado**, **probado localmente**, **desplegado**, **validado en
campo**. Nada de esta matriz está validado en campo.

Lo que NO se ejecutó y por eso no se afirma: impresoras, cajón, huella, corte
eléctrico, Windows empaquetado en una caja real, migraciones PENDIENTE contra
producción.

## Pedro (local-server) — requiere instalador nuevo

| Sev | Hallazgo | Corrección | Prueba | Estado |
|---|---|---|---|---|
| P0 | El poll de nube escribía un STATE_SYNC completo cada 5 s en `events.ndjson` (~220 MB/h); Pedro no volvía a arrancar tras un día | La foto de la nube es transitoria: memoria + broadcast sólo si cambia + `cloud-snapshot.json` reemplazable; arranque legacy sin internet rehidrata desde ahí | `foto-de-nube.test.js` 12/12 | probado localmente |
| P0 | Instalaciones existentes traen cientos de STATE_SYNC en el log; `toString` >512 MB lanza | Compactación al cargar (payload vacío, misma secuencia, última foto íntegra); carga línea por línea; reescritura frame por frame | idem | probado localmente |
| P1 | `markSynced` reescribía el log entero cada 5 s por cada delivery abierto; un fallo dejaba `_fault` | No se toca el disco si nada cambia; el poll no marca duplicados | idem | probado localmente |
| P1 | Una fila `cancelada`/mesa movida/turno cerrado en nube nunca corregía una orden local | La fila de nube cancela, mueve la mesa y retira la orden local YA COBRADA de otro turno; libera la mesa aunque estuviera "protegida" | `foto-de-nube-corrige-locales.test.js` 8/8 | probado localmente |
| P1 | La secundaria hidratada perdía `_from_cloud`: tableros divergentes | `from_cloud` viaja en la foto (sólo cuando es true) y se repone al hidratar | idem | probado localmente |
| P1 | Legacy: la comanda cobrada nunca salía del KDS de Pedro (no había acción de entrega) | Se conserva D2 (la cobrada se queda hasta entregarse; el lab legacy lo exige); `kds-ui` ofrece «Entregada» también sin Caja | idem | probado localmente |
| P1 | `kds-ui.html`: la segunda ronda jamás llegaba a `lista` | La llave de `postedStatus` lleva el número de rondas | idem (fuente) | probado localmente |
| P1 | T-09 a medias: los reenvíos HTTP/WS y la página del KDS usaban la IP congelada del config | `cajaActual.ip` vivo: nota de T-09 al arrancar y actualización al reubicar la caja | `reinicio-y-caja-movida.test.js` (dos servidores HTTP reales) | probado localmente |
| P1 | TURNO_CLOSED era best-effort (2.5 s); si se perdía, el empalme de ayer | Aviso durable desde el wizard; el poll pide por id las filas de las órdenes locales de otro turno; un TURNO_CLOSED tardío con otro turno abierto se ignora | idem + `cierre-z-limpia-el-dia` | probado localmente |
| P2 | El replay fechaba las comandas con la hora del arranque | `created_at`/`sent_at` con el ts del evento | idem | probado localmente |
| P2 | Efectos de impresión fallidos tras el commit: el reintento nunca transmitía | El duplicado que recupera los efectos hace el broadcast una vez | idem | probado localmente |

Suite de Pedro: **636/636**.

## Service Worker y cola offline — viaja por Vercel

| Sev | Hallazgo | Corrección | Prueba | Estado |
|---|---|---|---|---|
| P0 | `/api/pos/db?path=pos_orders…` (terminal con PIN) se cacheaba y servía como 200 con `ignoreSearch`: orden cobrada reaparecía abierta, turno cerrado resucitaba | El proxy hereda la política de la consulta: pos_orders/pos_mesas no se interceptan; el resto match exacto + marca `X-Fullsite-Stale`; cache v48 | `sw-proxy-pos-db.test.ts` (ejecuta `sw.js` en contexto falso; A/B 3/4) | probado localmente |
| P0 | Enviar sin red no guardaba `id`/`revision` en `pos_order_<mesa>`: reabrir inventaba otra orden o cobraba STALE | `recordarOrdenEncolada` en la rama OFFLINE_QUEUED | `pos-mesa-cache.test.ts` | probado localmente |
| P0 | Un 502 en el envío dejaba pasar el cobro de la misma orden → ORDER_NOT_FOUND terminal | Una orden se sube en orden (items posteriores se detienen sin consumir reintentos); ORDER_NOT_FOUND con creación en cola es transitorio; ids de cola estrictamente crecientes | `una-orden-sube-en-orden.test.ts` (código real + OCC simulado; A/B 4/4) | probado localmente |
| P1 | Buffer de emergencia sin `method` → PATCH 405 eterno | El buffer escribe POST; el drenado infiere POST para `/api/` | `buffer-de-emergencia-lleva-method.test.ts` | probado localmente |
| P1 | Sesión de dashboard ajena al tenant → 401 en cada drenado → deslogueo en bucle | Un 401 con la sesión se reintenta con el shift token; el pase sigue con él | `replay-401-reintenta-con-shift-token.test.ts` | probado localmente |
| P1 | La cache offline de PIN no revisaba rol: un capitán autorizaba como gerente 30 min | Gerente+ desde la cache; `verifyPinWithMinRole` exige el mínimo | `pin-cache-respeta-rol.test.ts` (A/B 2/3) | probado localmente |
| P1 | KDS web descartaba `items` en arreglo (DELTA del POS) | `normalizeOrder` serializa arreglos | `kds-normaliza-items-en-arreglo.test.ts` | probado localmente |

## Caja y cierre

| Sev | Hallazgo | Corrección | Prueba | Estado |
|---|---|---|---|---|
| P0 | `cola_pendiente_al_cerrar` sin migración: 400 tragado como «queued», turno cerrado SIN cierre | Migración `20260910090000`; fila del cierre en `pos-cierre-fila.ts` confrontada contra el esquema; **preflight** del POST antes de cerrar el turno local (un 4xx definitivo aborta y conserva el conteo) | `pos-cierre-fila-cabe-en-la-tabla.test.ts` | probado localmente; migración por aplicar en staging |
| P1 | `fecha` del cierre era UTC (todo Z de cena caía mañana) | `fechaDelCierre` = día de venta | idem | probado localmente |
| P1 | Cierre sin WAN cerraba con mesas vivas (guardia ciega) | La guardia lee de Pedro bajo Caja; la nube es respaldo | idem (fuente) | probado localmente |
| P2 | Movimientos de caja: la cola local sólo contaba si la nube devolvía 0 filas | Nube + IDB fusionados por id | idem | probado localmente |
| P2 | Abrir turno por encima de cuentas huérfanas si la consulta aún no contestaba | `handleOpen` vuelve a consultar y bloquea | `turno-gate-daily-z-contract` | probado localmente |
| P2 | Corte «por día» usa día natural 00:00; `pos_cierres.fecha` usa día de venta 05:00 | **No cambiado**: la ventana 00:00 fue decisión probada el 09-08 (`el-corte-del-dia-cubre-el-dia.test.ts`); queda registrada la divergencia | — | pendiente de decisión |

## Inventario — migraciones PENDIENTE (despliegue coordinado)

| Sev | Hallazgo | Corrección | Prueba | Estado |
|---|---|---|---|---|
| P0 | Fusionar mesas descontaba dos veces bajo la migración candidata | `r1_merge_orders` re-parenta `pos_reconciliation_results` y vacía el origen | lab PostgreSQL `run-inventory-reconcile-local` 13/13 | probado localmente (PostgreSQL local) |
| P0 | El cobro borraba los renglones cancelados (evidencia + disposición) → devolución ficticia de lo preparado | `r1_save_order` conserva los cancelados que el cliente no manda; STEP 5 de `r1_reconcile_order` falla cerrado ante huérfano con consumo y sin disposición | idem | probado localmente |
| P0 | Cancelar desde el KDS mandaba disposición `pending`: bloqueaba la orden entera | El KDS pregunta «¿ya se preparó?»; sin respuesta se conserva el consumo (nunca `pending`) | `cancelacion-confirmada.test.ts` | probado localmente |
| P1 | Stock negativo bloqueaba entradas y conteos | `pos_record_inventory_movement` valida costo/magnitud, no el signo | lab `run-inventory-movement-local` 8/8 | probado localmente |
| P1 | Intento congelado sin salida ante MOVEMENT_KEY_REUSED | Botón «Descartar este guardado» (decisión humana; el contrato de no olvidar solo se conserva) | `inventory-recovery.dom` | probado localmente |
| P1 | Cobro offline nunca descontaba market | Se encola `/api/pos/deduct-market` tras el cobro; RPC idempotente por orden; tenant r1 = no-op | lab 13/13 + fuente | probado localmente |
| P1 | Receta con insumo sin fila de inventario abortaba la orden entera (RAISE) | `BLOCKED_TARGET_MISSING` explícito | lab 13/13 | probado localmente |
| P2 | `recipe-sync` sumaba 100 g + 1 kg = 101 g | Conversión por tenant + métricas; 409 `MIXED_UNITS` si no hay | `recipe-sync-unidades.test.ts` | probado localmente |
| P2 | El KDS afirmaba «ingredientes devueltos» sin mover stock | El aviso usa `inventory_status` real | fuente | probado localmente |
| P2 | Toma física envía el delta calculado en el cliente, no el conteo absoluto | **No cambiado** (exige tipo de movimiento absoluto en la RPC) | — | pendiente |
| P2 | Recepción de orden de compra escribe stock absoluto leído en cliente | **No cambiado** | — | pendiente |

## Instalador

| Hallazgo | Corrección | Estado |
|---|---|---|
| La variable de GitHub `NEXT_PUBLIC_SUPABASE_ANON_KEY` es la llave de **staging** con la URL de **producción**: el instalador de CI sale con una pareja inservible | `build-offline-ui` y ambos workflows verifican que URL y llave anon sean del mismo proyecto y fallan con el comando exacto para corregirla | guard probado (12/12); **la variable la tiene que corregir Daniel** (bloqueo externo) |
| Binarios de huella (`DPUruNet.dll`, `fingerprint-service.exe`) no están en el repo | — | bloqueo externo: instalación nueva sin huella; AMALAY conserva los suyos en `C:\fullsite\` |

## Validación del commit final

Ver la última sección de [CIERRE-SOFTWARE-2026-09-10](CIERRE-SOFTWARE-2026-09-10.md).
