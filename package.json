{
  "name": "fullsite-warroom",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "certify:g01": "npm --prefix dashboard-app run certify:g01",
    "certify:selftest": "npm --prefix dashboard-app run certify:selftest",
    "lab:reset:g01": "npm --prefix dashboard-app run lab:reset:g01"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
